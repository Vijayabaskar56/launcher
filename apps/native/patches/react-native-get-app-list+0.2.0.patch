diff --git a/android/src/main/java/com/getapplist/GetAppListModule.kt b/android/src/main/java/com/getapplist/GetAppListModule.kt
index 1234567..abcdefg 100644
--- a/android/src/main/java/com/getapplist/GetAppListModule.kt
+++ b/android/src/main/java/com/getapplist/GetAppListModule.kt
@@ -32,7 +32,8 @@ class GetAppListModule(reactContext: ReactApplicationContext) :

       for (packageInfo in packages) {
         val applicationInfo = packageInfo.applicationInfo
-        if (applicationInfo != null && (applicationInfo.flags and ApplicationInfo.FLAG_SYSTEM) == 0
+        // Include all apps that have a launch intent (appear in the launcher)
+        if (applicationInfo != null && pm.getLaunchIntentForPackage(packageInfo.packageName) != null
         ) {
           val app: WritableMap = Arguments.createMap()
           app.putString("packageName", packageInfo.packageName)
