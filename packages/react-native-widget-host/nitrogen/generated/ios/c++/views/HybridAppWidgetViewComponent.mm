///
/// HybridAppWidgetViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridAppWidgetViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridAppWidgetViewSpecSwift.hpp"
#import "NitroWidgetHost-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::widgethost;
using namespace margelo::nitro::widgethost::views;

/**
 * Represents the React Native View holder for the Nitro "AppWidgetView" HybridView.
 */
@interface HybridAppWidgetViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridAppWidgetViewComponent {
  std::shared_ptr<HybridAppWidgetViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridAppWidgetViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridAppWidgetViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridAppWidgetViewSpec> hybridView = NitroWidgetHost::NitroWidgetHostAutolinking::createAppWidgetView();
    _hybridView = std::dynamic_pointer_cast<HybridAppWidgetViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroWidgetHost::HybridAppWidgetViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridAppWidgetViewProps const>(props);
  auto& newViewProps = const_cast<HybridAppWidgetViewProps&>(newViewPropsConst);
  NitroWidgetHost::HybridAppWidgetViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // appWidgetId: number
  if (newViewProps.appWidgetId.isDirty) {
    swiftPart.setAppWidgetId(newViewProps.appWidgetId.value);
    newViewProps.appWidgetId.isDirty = false;
  }
  // widgetWidth: number
  if (newViewProps.widgetWidth.isDirty) {
    swiftPart.setWidgetWidth(newViewProps.widgetWidth.value);
    newViewProps.widgetWidth.isDirty = false;
  }
  // widgetHeight: number
  if (newViewProps.widgetHeight.isDirty) {
    swiftPart.setWidgetHeight(newViewProps.widgetHeight.value);
    newViewProps.widgetHeight.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroWidgetHost::NitroWidgetHostAutolinking::isAppWidgetViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroWidgetHost::HybridAppWidgetViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  NitroWidgetHost::HybridAppWidgetViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
